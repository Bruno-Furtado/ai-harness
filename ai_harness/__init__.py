"""The ai-harness installer.

Installs the agents, skills, commands, hooks and rules of this repository into
every tool that can read them. The package is the single implementation behind
both `./harness` in a clone and the `harness` command installed from PyPI.
"""

__version__ = "0.3.0"
